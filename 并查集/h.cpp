#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;

int main() {
	int n, m;
	int x, y;
	char str[10];
	scanf("%d%d", &n, &m);
	bool flag = false;
	for(int i = 1; i <= m; i++) {
		scanf("%d%d%s", &x, &y, str);	
	}
	return 0;
}
